# mandelbrot-cuda

Simple Mandelbrot fractal GPU renderer.

## Build instructions

Windows:

```
.\build_cuda.bat
cargo build [--release]
```

Linux:

```
./build_cuda.sh && cargo build [--release]
```
